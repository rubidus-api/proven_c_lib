# AI Workflow

*   **TDD First**: Implement via failing unit tests in `/tests/` compiled via `nob.c`. Write the test, run `./nob`, observe failure, fix the C code, re-rerun.
*   **Documentation in English**: All comments, docs, and changelogs must strictly remain in English.
*   **Versioning**: Update `include/proven/version.h` and `CHANGELOG.md` upon modification.
