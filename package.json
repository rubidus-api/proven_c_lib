{
  "name": "proven-lib-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix docs-site",
    "build": "npm run build --prefix docs-site",
    "preview": "npm run preview --prefix docs-site",
    "test_c": "cc -D_DEFAULT_SOURCE -std=c23 nob.c -o nob && ./nob",
    "install-docs": "npm install --prefix docs-site",
    "postinstall": "npm run install-docs"
  }
}
