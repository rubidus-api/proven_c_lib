# Tests

Compact test documentation for `proven_c_lib`.

The detailed verification matrix remains in root `TEST.md`. This directory keeps
small catalog entries for repeatable project checks and process-level tests.

